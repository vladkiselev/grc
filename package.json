{
  "name": "car-rental-polygon-search",
  "version": "1.0.0",
  "private": true,
  "description": "Draw a polygon on a map and find car rental locations inside it via OSM/Overpass.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
